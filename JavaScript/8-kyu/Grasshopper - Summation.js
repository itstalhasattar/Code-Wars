var summation = function (num) {
    // Solution
    let result= 0;
    let counter=0;
    while (counter <= num){
      result = result + counter
      counter = counter+1
    }
    return result
  }
